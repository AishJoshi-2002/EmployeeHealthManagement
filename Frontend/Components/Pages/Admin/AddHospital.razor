@page "/add-hospital"
@using Frontend.Models
@using Frontend.Data
@using System.ComponentModel.DataAnnotations
@using Microsoft.AspNetCore.Components.Forms
@inject HttpClient Http
@inject IJSRuntime JS
@using System.Net.Http.Json
@rendermode InteractiveServer

<PageTitle>Add Hospital</PageTitle>

<div class="hospital-page">

    <div class="form-card">

        <h3 class="page-title">Add Hospital Master Data</h3>

        <EditForm Model="@hospitalModel" OnValidSubmit="HandleSubmit" FormName="AddHospitalForm">
            <DataAnnotationsValidator />
            @* <ValidationSummary /> *@

            <div class="form-grid">

                <!-- Hospital Id -->
                <div class="form-group">
                    <label>Hospital Id/Vendor Code<span>*</span></label>
                    <InputText class="form-control custom-input" @bind-Value="hospitalModel.hospitalId"
                               @bind:event="oninput" />
                    <ValidationMessage For="@(() => hospitalModel.hospitalId)" />
                </div>

                <!-- Hospital Name -->
                <div class="form-group">
                    <label>Hospital Name<span>*</span></label>
                    <InputText class="form-control custom-input" @bind-Value="hospitalModel.name"
                               @bind:event="oninput" />
                    <ValidationMessage For="@(() => hospitalModel.name)" />
                </div>

                <!-- Address 1 -->
                <div class="form-group">
                    <label>Hospital Address Line 1<span>*</span></label>
                    <InputText class="form-control custom-input" @bind-Value="hospitalModel.address1"
                               @bind:event="oninput" />
                    <ValidationMessage For="@(() => hospitalModel.address1)" />
                </div>

                <!-- Address 2 -->
                <div class="form-group">
                    <label>Hospital Address Line 2</label>
                    <InputText class="form-control custom-input normal-border" @bind-Value="hospitalModel.address2"
                               @bind:event="oninput" />
                </div>

                <!-- State -->
                <div class="form-group">
                    <label>State<span>*</span></label>
                    <InputSelect class="form-control custom-input" @bind-Value="hospitalModel.stateId"
                                 @bind-Value:after="OnStateChanged">
                        <option value="">--Select State--</option>
                        @foreach (var state in states)
                        {
                            <option value="@state.Id">
                                @state.Name
                            </option>
                        }
                    </InputSelect>
                    <ValidationMessage For="@(() => hospitalModel.stateId)" />
                </div>

                <!-- City -->
                <div class="form-group">
                    <label>City<span>*</span></label>
                    <InputSelect class="form-control custom-input" @bind-Value="hospitalModel.cityId"
                                 @bind-Value:after="OnCityChanged">
                        <option value="">--Select City--</option>
                        @foreach (var city in cities)
                        {
                            <option value="@city.Id">
                                @city.Name
                            </option>
                        }
                    </InputSelect>

                    <ValidationMessage For="@(() => hospitalModel.cityId)" />
                </div>

                <!-- Pincode -->
                <div class="form-group">
                    <label>Pincode<span>*</span></label>
                    <InputText class="form-control custom-input" @bind-Value="hospitalModel.pincode"
                               @bind:event="oninput" />
                    <ValidationMessage For="@(() => hospitalModel.pincode)" />
                </div>

                <!-- Contact No -->
                <div class="form-group">
                    <label>Contact No (LL)</label>
                    <InputText class="form-control custom-input normal-border" placeholder="e.g. 0124-0000000"
                               @bind-Value="hospitalModel.contactNoLL" @bind:event="oninput" />
                </div>

                <!-- Contact Person -->
                <div class="form-group">
                    <label>Contact Person<span>*</span></label>
                    <InputText class="form-control custom-input" @bind-Value="hospitalModel.contactPerson"
                               @bind:event="oninput" />
                    <ValidationMessage For="@(() => hospitalModel.contactPerson)" />
                </div>

                <!-- Contact Designation -->
                <div class="form-group">
                    <label>Contact Person Designation<span>*</span></label>
                    <InputText class="form-control custom-input" @bind-Value="hospitalModel.contactPersonDesignation"
                               @bind:event="oninput" />
                    <ValidationMessage For="@(() => hospitalModel.contactPersonDesignation)" />
                </div>

                <!-- Email -->
                <div class="form-group">
                    <label>Contact Person Email<span>*</span></label>
                    <InputText class="form-control custom-input" @bind-Value="hospitalModel.contactPersonEmail"
                               @bind:event="oninput" />
                    <ValidationMessage For="@(() => hospitalModel.contactPersonEmail)" />
                </div>

                <!-- Mobile -->
                <div class="form-group">
                    <label>Contact No (M)<span>*</span></label>
                    <InputText class="form-control custom-input" @bind-Value="hospitalModel.contactNoM"
                               @bind:event="oninput" />
                    <ValidationMessage For="@(() => hospitalModel.contactNoM)" />
                </div>

                <!-- Alternate Person -->
                <div class="form-group">
                    <label>Alternate Contact Person</label>
                    <InputText class="form-control custom-input normal-border"
                               @bind-Value="hospitalModel.altContactPerson" @bind:event="oninput" />
                </div>

                <!-- Alternate Designation -->
                <div class="form-group">
                    <label>Alternate Contact Person Designation</label>
                    <InputText class="form-control custom-input normal-border"
                               @bind-Value="hospitalModel.altContactPersonDesignation" @bind:event="oninput" />
                </div>

            </div>

            <div class="button-container">
                <button type="submit" class="save-btn">
                    Add Hospital
                </button>
            </div>

        </EditForm>

    </div>

</div>

@code {

    private HospitalModel hospitalModel = new();

    private List<StateModel> states = LocationData.States;

    private List<CityModel> cities = new();

    private void OnStateChanged()
    {
        if (hospitalModel.stateId != null)
        {
            var selectedState = states.FirstOrDefault(
                x => x.Id == hospitalModel.stateId
            );

            hospitalModel.stateName = selectedState?.Name;

            cities = LocationData.Cities
                .Where(c => c.StateId == hospitalModel.stateId)
                .ToList();

            // reset city
            hospitalModel.cityId = null;
            hospitalModel.cityName = null;
        }
    }

    private void OnCityChanged()
    {
        if (hospitalModel.cityId != null)
        {
            var selectedCity = cities.FirstOrDefault(
                c => c.Id == hospitalModel.cityId
            );

            hospitalModel.cityName = selectedCity?.Name;
        }
    }

    private async Task HandleSubmit()
    {
        Console.WriteLine("FORM SUBMITTED");
        Console.WriteLine(
        System.Text.Json.JsonSerializer.Serialize(hospitalModel));
        try
        {
            var response = await Http.PostAsJsonAsync(
                @* "https://jsonplaceholder.typicode.com/posts", *@
                "http://localhost:3000/api/hospital",
                hospitalModel
            );

            if (response.IsSuccessStatusCode)
            {
                Console.WriteLine("SUCCESS");

                // Alert popup
                await JS.InvokeVoidAsync(
                    "alert",
                    "Hospital Added Successfully"
                );

                // Clear form
                hospitalModel = new HospitalModel();

                StateHasChanged();
            }
            else
            {
                await JS.InvokeVoidAsync(
                    "alert",
                    "Failed to Add Hospital"
                );
            }
        }
        catch (Exception ex)
        {
            Console.WriteLine(ex.Message);
        }
    }
}